{
  "name": "@railplan/core",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "The constraint engine, network topology and resource model. No UI, no HTTP, no database.",
  "exports": {
    "./engine/*": "./src/engine/*.ts",
    "./domain/*": "./src/domain/*.ts",
    "./data/*": "./src/data/*.ts",
    "./types/*": "./src/types/*.ts"
  }
}
